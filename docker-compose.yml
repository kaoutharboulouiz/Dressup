services:
  db:
    image: pgvector/pgvector:pg16
    environment:
      POSTGRES_USER: dressing
      POSTGRES_PASSWORD: dressing
      POSTGRES_DB: dressing
    ports: ["5433:5432"]
    volumes: ["pgdata:/var/lib/postgresql/data"]
volumes:
  pgdata: